[package]
name = "js"
version = "0.1.0"
edition = "2021"

[dependencies]
common = { path = "../common" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
pnet = "0.35"
pcap = "2.2"

[dev-dependencies]
criterion = "0.5"

[members]
["examples/packet"]